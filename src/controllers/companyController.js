
//this is the req/res handler which will take the request body and return a response

const createCompany = async(req,res)=>{
    try {
        //pass the request body to the imported business logic function here which will return something as a response, so business logic function will have only one argument
    
        
    } catch (error) {
        
    }
}

module.exports= {createCompany}